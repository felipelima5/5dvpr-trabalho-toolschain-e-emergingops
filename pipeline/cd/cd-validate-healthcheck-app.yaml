version: 0.2

phases:
  pre_build:
    commands:
    - echo "Step Validate Health Check App"
  build:
    commands:
    - curl -X GET https://api.keephouseorder.net/healthcheck/ready

  post_build:
    commands:
    - echo "Step Finish"
