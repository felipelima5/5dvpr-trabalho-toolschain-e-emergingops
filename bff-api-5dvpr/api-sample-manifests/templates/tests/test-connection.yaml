apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-sample-manifests.fullname" . }}-test-connection"
  labels:
    {{- include "api-sample-manifests.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-sample-manifests.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
